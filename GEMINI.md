# قواعد البرمجة الصارمة ومنع الهلوسة (Strict Programming Rules)

* يمنع منعاً باتاً استخدام مصفوفات ثابتة (Mock arrays) أو أكواد تخمين عشوائي (Dummy Classifier) لتجنب انقطاع البيانات.
* عند تعديل أي ملف بايثون، يجب تشغيل أداة التجميع للتأكد من سلامة البناء:
  `.venv\Scripts\python.exe -m py_compile <اسم_الملف>`
* يجب التحقق من صحة التغييرات في قاعدة البيانات عبر استعلام حقيقي وعرض النتائج.
* الالتزام التام بالشفافية؛ إذا كانت الخدمة متوقفة أو البيانات غير متاحة، يتم عرض تنبيه صريح بالواجهة بدلاً من استنباط نسب تخمينية.
* سيتم تحميل هذه القواعد تلقائياً في كل دورة تواصل لفرض الامتثال البرمجي التام.
